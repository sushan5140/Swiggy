{
  "name": "intent-to-transaction-verification",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Independent synthetic grocery intent contract verifier, not a Swiggy integration",
  "scripts": {
    "check:fixtures": "node scripts/validate_day01_fixtures.mjs",
    "test": "node --test tests/*.test.mjs",
    "benchmark": "node scripts/run_benchmark.mjs",
    "audit": "node scripts/audit.mjs",
    "demo": "node demo/server.mjs",
    "verify": "npm run check:fixtures && npm test && npm run benchmark && npm run audit"
  },
  "engines": {
    "node": ">=20"
  }
}
